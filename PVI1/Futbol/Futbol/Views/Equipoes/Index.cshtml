@model IEnumerable<Futbol.Models.Equipo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Liga.nombreLiga)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombreEquipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombreEstadio)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Liga.nombreLiga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombreEquipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombreEstadio)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.equipoID }) |
                @Html.ActionLink("Details", "Details", new { id=item.equipoID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.equipoID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
